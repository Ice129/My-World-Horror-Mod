package horror.blueice129.utils;

/**
 * Utility class for entity-related operations
 * will be filled out when entity is added
 */
public class EntityUtils {

    /**
     * Returns true if the entity is within proximity blocks of the player.
     * Placeholder: Replace Object with actual Entity and Player types when available.
     */
    public static boolean isEntityNearPlayer(Object entity, Object player, int proximity) {
        // to be implemented when entity and player classes are added
        return false;
    }
}
